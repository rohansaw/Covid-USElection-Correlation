def plot_party_cases(df_polls, df_covid):
    df_biden = df_polls
    df_trump = df_polls

def buildmap():
    # can select if I want to see republican or democratic states
    # then colour states with colortone red-green according to covid numbers
    # color only the states where selected party is leading